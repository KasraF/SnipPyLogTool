""" 
Count Duplicates
    Return the number of characters
    that appear in the input more
    than once.
    >>> task('aabbcde') == 2
    >>> task('book') == 1
    >>> task('vscode') == 0
"""
def task(inp):
    count = inp
    return 0

task('aabbcde')
